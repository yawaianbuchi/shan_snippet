export type ProjectType = {
  name: string;
  drawerWidth: number;
  version?: string;
};

export type Colors = {
  primary: string;
  secondary: string;
  text: string;
  white: string;
  black: string;
  gray: {
    100: string;
  };
};
